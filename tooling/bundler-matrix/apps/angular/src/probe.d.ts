declare module '@embedpdf/bundler-probe' {
  export function runProbe(root: HTMLElement): Promise<unknown>;
}
declare module '@embedpdf/bundler-probe/portable' {
  export function runProbe(root: HTMLElement): Promise<unknown>;
}
