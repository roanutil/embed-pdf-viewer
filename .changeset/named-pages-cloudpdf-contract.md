---
'@cloudpdf/contract': minor
---

Add `doc.pages.setName` and `doc.pages.removeName` to the public contract for registering, renaming, and removing named pages (`/Names /Pages` entries) on a layer.
