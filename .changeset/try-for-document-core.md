---
'@embedpdf/core': minor
---

`PluginContext.tryForDocument(token, documentId)` — `forDocument` for an OPTIONAL dependency: null when the plugin is not installed or that document is not ready, never a throw (a workspace plugin reaching a sibling's document-scoped lens without requiring it).
